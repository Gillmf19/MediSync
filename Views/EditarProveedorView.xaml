<Window x:Class="MediSync.Views.EditarProveedorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Proveedor" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Editar Proveedor"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Grid.Row="0" Margin="0,10,0,20"/>

        <!-- Campos de entrada -->
        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Nombre del Proveedor:" FontWeight="Bold"/>
            <TextBox x:Name="txtNombre" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Contacto:" FontWeight="Bold"/>
            <TextBox x:Name="txtContacto" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,5">
            <TextBlock Text="Correo Electrónico:" FontWeight="Bold"/>
            <TextBox x:Name="txtCorreo" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,5">
            <TextBlock Text="Teléfono:" FontWeight="Bold"/>
            <TextBox x:Name="txtTelefono" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,5">
            <TextBlock Text="Dirección:" FontWeight="Bold"/>
            <TextBox x:Name="txtDireccion" Width="300"/>
        </StackPanel>

        <!-- Botones de acción -->
        <StackPanel Grid.Row="6" Orientation="Vertical" HorizontalAlignment="Center" Margin="10">
            <Button Content="Guardar Cambios" Width="200" Height="40"
                    Background="#1A5276" Foreground="White" FontWeight="Bold"
                    Click="BtnGuardar_Click"/>
            <Button Content="Cancelar" Width="200" Height="40"
                    Background="Gray" Foreground="White" FontWeight="Bold"
                    Click="BtnCancelar_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
