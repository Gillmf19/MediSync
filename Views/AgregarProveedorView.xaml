<Window x:Class="MediSync.Views.AgregarProveedorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agregar Proveedor" Height="450" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Agregar Proveedor" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,10"/>

        <!-- Nombre del Proveedor -->
        <StackPanel Grid.Row="1" Margin="0,5,0,5">
            <TextBlock Text="Nombre del Proveedor:" FontWeight="Bold"/>
            <TextBox x:Name="txtNombre" Height="30"/>
        </StackPanel>

        <!-- Contacto -->
        <StackPanel Grid.Row="2" Margin="0,5,0,5">
            <TextBlock Text="Contacto:" FontWeight="Bold"/>
            <TextBox x:Name="txtContacto" Height="30"/>
        </StackPanel>

        <!-- Correo Electrónico -->
        <StackPanel Grid.Row="3" Margin="0,5,0,5">
            <TextBlock Text="Correo Electrónico:" FontWeight="Bold"/>
            <TextBox x:Name="txtCorreo" Height="30"/>
        </StackPanel>

        <!-- Teléfono -->
        <StackPanel Grid.Row="4" Margin="0,5,0,5">
            <TextBlock Text="Teléfono:" FontWeight="Bold"/>
            <TextBox x:Name="txtTelefono" Height="30"/>
        </StackPanel>

        <!-- Dirección -->
        <StackPanel Grid.Row="5" Margin="0,5,0,5">
            <TextBlock Text="Dirección:" FontWeight="Bold"/>
            <TextBox x:Name="txtDireccion" Height="30"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="7" Orientation="Vertical" HorizontalAlignment="Center" Margin="10">
            <Button Content="Guardar Proveedor" Width="200" Height="40" Margin="5"
                    Background="#1A5276" Foreground="White" FontWeight="Bold"
                    Click="BtnGuardar_Click"/>
            <Button Content="Cancelar" Width="200" Height="40" Margin="5"
                    Background="Gray" Foreground="White"
                    Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
